import { Routes } from "@angular/router";
import { CenterStateComponent } from "./center-state.component";

export const CENTER_STATE_ROUTES: Routes = [
    { path: '', component: CenterStateComponent }
];